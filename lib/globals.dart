import 'package:flame/components.dart';

/// Class representing global variables in the game.
class Globals {

  /// The width and height of one sprite in a sprite sheet.
  static final Vector2 spriteSize = Vector2.all(32);
}